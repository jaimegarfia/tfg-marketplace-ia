/** @deprecated Usa `@/context/auth-context`. Reexportación de compatibilidad. */
export {
  AuthProvider,
  AuthProvider as MockAuthProvider,
  useAuth,
  useAuth as useMockAuth,
  type AuthIntent,
  type AuthUser,
  type AuthUser as MockUser,
} from "@/context/auth-context";
